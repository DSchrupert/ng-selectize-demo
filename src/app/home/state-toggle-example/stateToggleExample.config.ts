import {DEFAULT_DROPDOWN_CONFIG} from '../../selectize.configs';

export const STATE_TOGGLE_EXAMPLE_CONFIG = Object.assign({}, DEFAULT_DROPDOWN_CONFIG, {
	labelField: 'label',
	valueField: 'value'
});

